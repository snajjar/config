<template name="GalleryPage">
    <section class="dualbox-examples-title mb-5">
        <div class="container">
            <div class="row">
                <div class="col-md-12">
                    <h1>Gallery</h1>
                </div>
            </div>
        </div>
    </section>

    <section class="dualbox-examples">
        <div class="container-fluid">
            <div class="row">
                {{#each getApps}}
                <div class="col-xs-12 col-sm-6 col-md-6 col-lg-3">
                    <div class="card">
                        <div class="card-header">
                            <h3><b>{{#if title}}{{title}}{{else}}No title{{/if}}</b></h3>
                        </div>
                        <div class="card-body">
                            {{#if descriptionImage}}
                                <img src="{{descriptionImage}}" height="194" style="padding:16px;">
                            {{else}}
                                <img src="/img/ressources/DeployedApps_Big_Icon_success.png" height="194" style="padding:16px;">
                            {{/if}}
                            <p class="trust-paragraph">
                                {{#if description}}{{description}}{{else}}No description{{/if}}
                                {{#each tags}}
                                <span class="badge badge-secondary">{{this}}</span>
                                {{/each}}
                            </p>
                            <a class="btn btn-success" href="/apps/{{name}}/production" target="_blank">Try it!</a>
                        </div>
                    </div>
                </div>
                {{/each}}
             </div>
        </div>
    </section>
</template>
